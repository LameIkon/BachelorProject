using UnityEngine;

public class TriggerDetector : MonoBehaviour
{
    //[SerializeField] private Placeable _parentSlot;

    //private void Awake()
    //{
    //    _parentSlot = GetComponentInParent<Placeable>();
    //}

    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.TryGetComponent(out IPickable pickup))
    //    {
    //        _parentSlot.OnPickupEnterTrigger(pickup);
    //    }
    //}

    //private void OnTriggerExit(Collider other)
    //{
    //    if (other.TryGetComponent(out IPickable pickup))
    //    {
    //        _parentSlot.OnPickupExitTrigger(pickup);
    //    } 
    //}
}
