/// <summary>
/// Uniq id for each page in the compendium
/// </summary>
public enum CompendiumID
{
    None,

    // Oven
    oven,
    StartProtocol,
    EndProtocol,

    // Terminals & Panels
    MainTerminal,
    EndTerminal,
    Lever,
    RestartPanel,
    ControlPanel,

    // Errors
    OccurenceOfError,
    OvenError,
    SensorError,

    // Equipment

    // Materials
    iceDry,
}
